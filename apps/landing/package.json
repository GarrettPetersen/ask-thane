{
  "name": "@ask-thane/landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "build": "echo 'Static Pages app: no build step required (serves from public/)'",
    "typecheck": "echo 'No TypeScript source in landing app yet'",
    "lint": "echo 'No lint task yet for @ask-thane/landing'",
    "test": "echo 'No tests yet for @ask-thane/landing'"
  },
  "devDependencies": {
    "wrangler": "^4.17.0"
  }
}
