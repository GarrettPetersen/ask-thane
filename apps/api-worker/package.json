{
  "name": "@ask-thane/api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint task yet for @ask-thane/api-worker'",
    "test": "vitest run"
  },
  "dependencies": {
    "@ask-thane/ai": "workspace:*",
    "@ask-thane/data": "workspace:*",
    "@ask-thane/domain": "workspace:*",
    "@ask-thane/workflows": "workspace:*",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "wrangler": "^4.17.0"
  }
}
