{
  "name": "@ask-thane/payments-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint task yet for @ask-thane/payments-worker'",
    "test": "pnpm -w exec vitest run apps/payments-worker/test"
  },
  "devDependencies": {
    "wrangler": "^4.17.0"
  }
}
