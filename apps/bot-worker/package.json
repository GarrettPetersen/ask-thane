{
  "name": "@ask-thane/bot-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint task yet for @ask-thane/bot-worker'",
    "test": "vitest run --no-file-parallelism --maxWorkers 1"
  },
  "dependencies": {
    "@ask-thane/ai": "workspace:*",
    "@ask-thane/data": "workspace:*",
    "@ask-thane/domain": "workspace:*",
    "@ask-thane/integrations": "workspace:*",
    "@ask-thane/workflows": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.5",
    "wrangler": "^4.17.0"
  }
}
