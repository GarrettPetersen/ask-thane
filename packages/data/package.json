{
  "name": "@ask-thane/data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint task yet for @ask-thane/data'",
    "test": "pnpm -w exec vitest run packages/data/test"
  },
  "dependencies": {
    "@ask-thane/domain": "workspace:*"
  }
}
