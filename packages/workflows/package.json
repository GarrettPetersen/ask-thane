{
  "name": "@ask-thane/workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint task yet for @ask-thane/workflows'",
    "test": "pnpm -w exec vitest run packages/workflows/test"
  },
  "dependencies": {
    "@ask-thane/ai": "workspace:*",
    "@ask-thane/data": "workspace:*",
    "@ask-thane/domain": "workspace:*"
  }
}
